# SME_Experiment2_Final

학번/이름 : 12233998 / 장원정

# 1. 모티베이션 & 인트로

## 1.1 중간발표 알고리즘을 그대로 사용하지 않은 이유

중간발표에서 제안한 알고리즘은 UWB와 WiFi라는 서로 다른 물리 특성을 갖는 두 센서의 측정 합의도(agreement)를 이용하는 SAFL 계열 구조였다. 해당 설계는 "서로 다른 센서가 동시에 동일한 위치를 지지할수록 신뢰도가 높다"는 이종 센서 융합 가정에 기반한다.

그러나 Final Project 데이터는 18개 기지국이 모두 동일한 RTT 측정만 제공하는 단일(homogeneous) 구조이다. 따라서 중간발표의 핵심 가정인 이종 센서 합의 자체가 성립하지 않는다.

또한 공간 규모 역시 중간발표의 약 12 m × 17 m 수준에서 약 100 m × 40 m 규모로 크게 증가하였으며, 측정 오차의 성격도 달라졌다. 중간발표 환경에서는 수 m 수준의 편향만 존재했지만, Final 데이터에서는 평균 +15.9 m 수준의 강한 NLOS 편향이 관측되었다.

따라서 중간발표 산출물을 단순히 발전시키는 것이 아니라, RTT 기반 환경의 물리적 특성에 맞추어 알고리즘을 처음부터 다시 설계하였다.

## 1.2 중간발표 피드백의 반영

중간발표에서 받은 주요 피드백은 다음과 같으며, 최종 설계와 보고서 작성 과정에 모두 반영하였다.

 첫째, Part 2·3의 이종 센서 합의도 기반 설계에 물리적 모순이 있다. 둘째, 파이프라인이 과도하게 복잡하고 불필요한 구성 요소가 포함되어 있다. 반영 내용은 다음과 같다.
| 피드백             | 반영 내용                                                    |
| --------------- | -------------------------------------------------------- |
| Part 2·3 물리적 모순 | 센서 구분이 없으므로 Part 2(이종 센서 합의도)·Part 3(boost 기반 가중치) 완전 제거 |
| 불필요한 구성 요소      | 각 요소를 ablation으로 검증하여 기여 없는 요소 제거                        |
| 파이프라인 복잡도       | 5단계 → 3단계로 축소. 이후 데이터 분석 결과에 근거하여 더 단순한 구조로 재설계          |

Part 2·3 제거 후 나머지(bias보정 + Grid + Huber + IRWLS)를 유지하여 측정한 결과 MAE 9.55 m였다. 중간발표 대비 크게 저하되어 근본 원인 파악을 위해 데이터 분석을 수행하였다.

## 1.3 데이터 분석과 핵심 관찰

700명의 학습 데이터를 분석한 결과 가장 중요한 사실은 RTT 측정이 실제 거리보다 커지는 방향으로 강하게 편향되어 있다는 점이었다.

* 평균 NLOS bias : +15.93 m
* 중앙값 NLOS bias : +10.15 m
* bias > 0 비율 : 81.4 %
* bias > 5 m 비율 : 59.0 %

즉 대부분의 측정은 실제 거리보다 크게 관측된다. 이는 NLOS 환경에서 신호가 장애물을 우회하거나 반사되어 더 긴 경로를 통과하기 때문이다.

이 사실로부터 두 가지 설계 원리를 도출하였다.

첫째, 상대적으로 작은 측정값일수록 LOS에 가까울 가능성이 높다. 이는 신뢰할 수 있는 측정 집합을 선택하는 consensus 단계의 근거가 된다.

둘째, 측정값은 실제 거리의 상한(upper bound)으로 해석할 수 있다. 이는 위치 추정 결과가 물리적으로 가능한 영역 안에 존재하도록 제약하는 근거가 된다.

본 연구의 모든 설계는 이 두 원리에서 출발한다.

## 1.4 알고리즘 개요

알고리즘은 세 단계의 결정적(deterministic) 직렬 구조로 구성된다.

첫째, 물리 필터를 통해 탐색 영역을 제한하고 비정상 측정을 제거한다.

둘째, 모든 3-anchor 조합에 대해 closed-form 삼변측량을 수행한 뒤, 전체 측정과 가장 높은 합의를 이루는 후보를 선택한다.

셋째, 선택된 inlier 측정은 최소제곱으로 정밀화하고, consensus에서 제외된 측정은 폐기하지 않고 NLOS 상한 제약으로 재활용한다.

따라서 본 알고리즘은 동일한 물리 사실(NLOS에 의한 거리 과대측정)을 이용하여 측정 선택과 위치 제약을 동시에 수행하며, 강한 NLOS 환경에서도 안정적인 위치 추정을 달성한다.

---

# 2. 알고리즘 설명

입력은 RTT 측정 거리 벡터 d = (d_1, ..., d_18)와 기지국 좌표 B = {b_i = (x_i, y_i) | i = 1,...,18}이다. 출력은 추정 위치 x_hat = (x, y)이다. 기하 거리를 rho_i(q) = ||q - b_i||, 잔차를 r_i(q) = rho_i(q) - d_i로 정의한다.

## 2.1 격자 및 탐색 경계 초기화

기지국 좌표 범위에 각 방향 30% 마진을 더하여 탐색 경계를 설정한다.

B_x = [x_min - 0.30 * W_x, x_max + 0.30 * W_x], W_x = x_max - x_min

B_y = [y_min - 0.30 * W_y, y_max + 0.30 * W_y], W_y = y_max - y_min

경계 내에 0.5 m 간격의 2차원 격자 G를 구성한다. main() 진입 시 1회 생성하여 모든 UE 추론에 재사용한다. GT 위치나 학습 데이터가 필요 없다.

## 2.2 Step 1. Minimax — Chebyshev Center 탐색

### 목적함수

d_hat[i] >= d_true[i]이므로 실제 위치는 D_i = {q : ||q - b_i|| <= d_i}의 교집합 내부에 존재한다. slack을 s_i(q) = ||q - b_i|| - d_i로 정의하면 s_i < 0은 원 내부, s_i > 0은 위반이다. Chebyshev Center는 최악 위반을 최소화한다.

f(q) = min_q max_i (||q - b_i|| - d_i)

max 연산은 미분 불가능하므로 alpha = 10의 log-sum-exp로 smooth하게 근사한다.

f_smooth(q) = (1 / alpha) * log( sum_{i=1}^{18} exp(alpha * s_i(q)) )

### 수식 변형 실험

팀원으로부터 수식 변형 피드백을 받아 네 가지를 실험하였다.

| 수식 형태 | LTS+Fallback 결합 시 MAE |
|---|---:|
| v1: max(||q-b_i|| - d_i) [채택] | 4.800 m |
| v2: max \| ||q-b_i|| - d_i \| | 26.293 m |
| v3: max(0, ||q-b_i|| - d_i) | 5.920 m |
| v4: max(0, d_i - ||q-b_i||) | 44.330 m |

v4는 NLOS로 부풀려진 측정값에 직접 수렴하는 방향으로 q를 끌어당겨 가장 나쁜 결과를 보였다. v1이 원 내부 깊이를 최대화하여 NLOS 성질을 올바르게 반영하므로 채택하였다.

### 최적화 절차

격자 G에서 f_smooth를 평가하여 최솟값 격자점을 초기점 q_0으로 선택한다. 목적함수가 비볼록하므로 격자 탐색으로 전역 최솟값 근방을 포착한 뒤, L-BFGS-B로 정밀화하여 p_minimax를 확정한다.

## 2.3 Step 2. LTS — 잔차 하위 k개 앵커에만 fit

### 목적함수

p_minimax는 안전한 내부점이지만 실제 위치와 수 m 거리가 있을 수 있다. LTS는 18개 잔차 제곱값을 오름차순 정렬한 r_(1)^2 <= ... <= r_(18)^2에서 하위 k개의 합만 최소화한다.

L_LTS(q) = sum_{j=1}^{k} r_(j)^2(q)

k = 7로 설정하면 NLOS가 심한 나머지 11개 앵커가 목적함수에서 완전히 제외된다. p_minimax를 초기점으로 L-BFGS-B로 최소화하여 p_lts를 확정한다.

### k = 7 선정 근거와 Huber 대비 LTS 선택 이유

k = 7은 bias > 5 m 비율이 59.0%라는 데이터 분석 결과에 근거한다. 전체의 과반이 심한 NLOS이므로 하위 약 39%(7/18)만 신뢰하는 것이 적합하다. k = 5, 7, 9 실험에서 k = 7이 MAE와 2m이내 비율의 균형이 가장 좋았다.

Huber Loss 대신 LTS를 선택한 이유는 bias 보정 없이 raw d_hat을 사용하면 잔차가 Huber 전환점(5 m)을 크게 넘는 경우가 많아 Huber의 선형 구간이 충분한 억제 효과를 발휘하지 못하기 때문이다. LTS는 k개 앵커를 직접 선택하여 나머지를 완전히 배제하므로 이 상황에 더 적합하다.

## 2.4 Step 3. Slack Fallback — 물리 성질 위반 처리

LTS에서 강한 NLOS 앵커 방향으로 끌려가 원 밖으로 이탈하는 경우가 700명 중 207명(29.6%)에서 발생하였다. p_lts의 최대 slack을 계산하여 판정한다.

max_slack = max_{i=1,...,18} (||p_lts - b_i|| - d_i)

max_slack > 5 m이면 NLOS 물리 성질 위반으로 판단하여 p_minimax를 반환하고, 그렇지 않으면 p_lts를 반환한다. threshold 5 m는 LOS 잡음 수준(|bias| < 1 m 비율 29.9%)을 고려하여 작은 위반은 허용하되 명확한 이탈만 차단하도록 설정하였다.

---

# 3. Agent AI 활용 방안

Claude(Anthropic)를 주요 Agent AI로 활용하였다.

## 3.1 역할 분담

| 작업 영역 | 본인이 수행한 것 | Claude가 수행한 것 |
|---|---|---|
| 방향 결정 | SAFL 폐기 판단, Minimax 아이디어 도출, 비지도 원칙 유지 결정 | 방법론 개념 설명 및 선택지 제시 |
| 실험 설계 | 어떤 실험을 할지 결정, baseline 구성 | 코드 작성 및 수치 측정 |
| 수식 선택 | v4 피드백 검토 후 v1 유지 결정 | v1~v4 구현·비교 실행 |
| 파라미터 결정 | k=7, threshold=5 m 물리적 근거 도출 | 파라미터 범위별 실험 수행 |
| 방법론 기각 | k-NN 적응 bias의 비지도 원칙 위배 판단 | k-NN 방법 제안 및 구현 |
| 코드 작성 | 알고리즘 설계 방향 지시, 규격 검토 | main.py 구현 |

## 3.2 구체적 상호작용 사례

삼각부등식 실험에서 Claude가 "LOS 앵커 38.4% 잘못 제거, d_corr 표준편차 32.9 m, 앵커 간 최대 거리 107.7 m"를 제시하였다. 이 수치를 보고 본인이 측정값 간 기하 일관성 기반 접근의 근본 한계를 판단하고 Minimax 방향으로 전환하였다.

수식 변형에서 팀원으로부터 v4가 더 자연스럽다는 피드백을 받았다. Claude가 v1~v4를 구현하고 v4가 MAE 44.33 m로 가장 나쁨을 측정하였다. v4가 NLOS 측정값에 직접 수렴하는 방향으로 작동한다는 분석을 함께 수행하여 본인이 v1 유지를 결정하였다.

Claude가 k-NN 적응 bias 보정 방법을 제안하였고 실험 결과 MAE 7.76 m의 개선이 있었다. 그러나 이 방법이 학습 데이터 GT 위치를 직접 참조하여 완전 비지도 원칙에 위배된다는 판단으로 본인이 채택을 거부하였다.

---

# 4. 결과 도출 & 디스커션

## 4.1 자체 평가 방식과 공정성

본 알고리즘은 GT 위치를 사용하지 않으므로 학습/테스트 분리 없이 700명 전체를 평가에 사용하였다. 완전 비지도 구조이므로 700명 전체 평가가 hidden test set 성능의 공정한 근사이다.

비교 baseline은 비지도 또는 통계량만 사용하는 방법으로 구성하였다.

| 비교 대상 | 방법 설명 | GT 사용 수준 |
|---|---|---|
| Simple LS | raw d_hat으로 최소제곱 삼변측량 | 없음 |
| Bias보정 + LS | 앵커별 median bias 차감 후 최소제곱 | 통계량 간접 사용 |
| SAFL (중간발표) | Bias보정 + Grid + Huber + IRWLS | 통계량 간접 사용 |
| Minimax 단독 | Step 1만 수행 (ablation) | 없음 |
| 최종 알고리즘 | Minimax + LTS(k=7) + Slack Fallback | 없음 |

## 4.2 성능 수치

| 방법 | MAE (m) | Median (m) | RMSE (m) | P90 (m) | 2m 이내 (%) |
|---|---:|---:|---:|---:|---:|
| Simple LS (raw d_hat) | 39.352 | 27.054 | 61.542 | 72.629 | 0.3 |
| Bias보정 + LS | 32.479 | 21.106 | 53.670 | 58.971 | 0.4 |
| SAFL (중간발표) | 9.553 | 8.195 | 11.790 | 17.664 | 3.6 |
| Minimax 단독 (Step 1) | 6.727 | 5.270 | 9.210 | 15.324 | 35.3 |
| 최종 알고리즘 (Full) | 4.800 | 1.261 | 7.990 | 14.714 | 58.7 |

## 4.3 결과 해석

Bias보정 + LS에서 MAE 감소가 제한적인 것은 위치 의존적 NLOS를 고정 통계량으로 보정할 수 없다는 증거이다. SAFL이 9.55 m로 줄어든 것은 Huber Loss와 IRWLS의 outlier 억제 덕분이지만, bias 추정 한계를 근본적으로 해결하지 못하였다.

핵심 관찰은 Minimax 단독이 bias 보정 없이도 SAFL보다 2.8 m 우수하다는 것이다. Chebyshev Center는 LOS에 가까운 앵커들의 원 교집합 방향으로 자연스럽게 수렴하며, 위치 의존적 편차가 큰 고정 bias 보정보다 더 신뢰할 수 있는 기준을 제공한다.

LTS 추가 후 Median이 5.27 m에서 1.26 m로 개선되고 2m이내 비율이 58.7%로 상승한다. 700명 중 493명(70.4%)에서 LTS가 정상 작동하고, 207명(29.6%)에서 Slack Fallback이 p_minimax로 복귀한다.

가장 공정한 비교는 GT를 전혀 사용하지 않는 Simple LS와의 비교이다. MAE 39.35 m에서 4.80 m로의 개선이 알고리즘의 실질적 기여를 보여준다. Bias보정 계열보다 불리한 조건에서도 SAFL보다 MAE와 Median 모두 우수하였다.

## 4.4 장점과 한계

장점으로는 완전 비지도 구조로 과적합 위험이 없고, NLOS 양의 편향 성질만을 가정하므로 환경이 달라져도 이 성질이 유지되는 한 안정적이다. 파라미터가 alpha = 10, k = 7, threshold = 5 m 세 개뿐이며 모두 물리적 근거로 선정되었다. 1000명 처리 예상 시간이 약 15초로 10분 제한 대비 충분하다.

한계로는 LTS의 k = 7이 11개 이하가 NLOS outlier라는 가정에 기반하여, 극단적 환경에서 12개 이상이 심한 NLOS이면 실패할 수 있다. P90이 14.71 m로 tail 케이스에서 오차가 잔존한다.

## 4.5 향후 개선 방향

k를 고정하는 대신 Minimax 결과에서 잔차 MAD를 기준으로 샘플별 동적 결정하는 방법을 고려할 수 있다. Slack Fallback threshold도 해당 샘플의 d_hat 중앙값에 비례하여 설정하면 측정 규모가 다른 환경에서 더 강건해진다.

---

# 5. Reference

[1] P. C. Chen, "A non-line-of-sight error mitigation algorithm in location estimation," Proc. IEEE Wireless Communications and Networking Conference (WCNC), 1999.

이 논문은 NLOS 측정값이 실제 거리보다 크게 측정된다는 현상을 분석하고, 잔차의 분산·왜도를 이용한 hypothesis test로 NLOS를 식별한 뒤 보정 계수를 적용하는 방법을 제안한다.

본 알고리즘이 가져온 것은 d_hat[i] >= d_true[i]라는 물리적 사실이다. 이로부터 ||x_true - b_i|| <= d_hat[i]가 도출되며, §2.2의 slack s_i(q)와 Minimax 목적함수의 직접적 근거가 되었다.

달리 한 것은 NLOS 식별과 보정 과정을 제거한 점이다. 이 논문은 NLOS를 이진 분류 후 보정하지만, 본 알고리즘은 분류 없이 d_hat >= d_true 성질을 목적함수에 직접 반영한다. 이 차이가 GT 없이 동작하는 비지도 구조를 가능하게 하며, 식별 실패에 의한 성능 저하 위험도 없앤다.

[2] S. Maranò, W. M. Gifford, H. Wymeersch, and M. Z. Win, "NLOS Identification and Mitigation for Localization Based on UWB Experimental Data," IEEE Journal on Selected Areas in Communications, vol. 28, no. 7, pp. 1026-1035, 2010.

이 논문은 UWB 실험 데이터에서 NLOS 측정값이 양의 편향과 큰 분산을 가진다는 특성을 실증하고, 이를 단방향 상한 제약으로 처리하는 아이디어를 제시한다. 실제 위치가 측정 원 내부에 있어야 한다는 feasibility 개념이 포함된다.

본 알고리즘이 가져온 것은 측정값을 실제 거리의 상한으로 해석하는 관점이다. 이것이 §2.2에서 18개 측정 원 교집합 내부를 탐색하는 이론적 근거이다. 본 데이터 분석에서 bias > 0 비율 81.4%를 확인하여 이 논문의 실증 결과가 본 데이터에도 성립함을 검증하였다.

달리 한 것은 NLOS 식별 없이 18개 전체를 Minimax로 동시에 처리한다는 점이다. 이 논문이 식별된 측정에만 제약을 부과하는 2단계 방식인 반면, 본 알고리즘은 LTS가 사후적으로 앵커를 잔차 기준으로 배제한다. 또한 이 논문이 동적 트래킹을 대상으로 하는 반면, 본 알고리즘은 단일 시점 정적 위치 추정에 집중하여 격자 탐색 + L-BFGS-B로 경량하게 구현하였다.

[3] P. J. Huber, "Robust Estimation of a Location Parameter," Annals of Mathematical Statistics, vol. 35, no. 1, pp. 73-101, 1964.

이 논문은 M-estimator 개념을 도입하여 outlier에 강건한 추정의 이론적 토대를 마련한다. 잔차 임계값 이하에서는 제곱 손실, 초과에서는 선형 손실을 적용하는 Huber Loss가 대표적이다.

본 알고리즘이 가져온 것은 outlier를 잔차 크기에 따라 차등 취급하는 robust estimation 철학이다. 중간발표 SAFL의 Part 4에서 Huber Loss를 직접 구현하였으며, 이것이 이 논문의 직접 적용이었다.

달리 한 것은 최종 알고리즘에서 Huber Loss 대신 LTS를 채택한 점이다. Huber Loss는 outlier에 선형 벌점을 부여하여 영향이 남지만, LTS는 하위 k개만 fit하여 나머지를 완전히 배제한다. bias 보정 없이 raw d_hat을 사용하면 잔차가 Huber 전환점(5 m)을 크게 넘는 경우가 많아 Huber의 효과가 제한적이다. LTS가 이 상황에 더 적합하며 실험으로도 Median 지표에서 의미 있는 개선이 확인되었다.
