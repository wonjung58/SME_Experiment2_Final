# SME_Experiment2_Final

학번/이름 : 12233998 / 장원정

# 1. 모티베이션 & 인트로

## 1.1 중간발표 알고리즘과 Final Project 데이터의 괴리

중간발표에서는 팀 알고리즘 SAFL(Sensor-Agreement Fused Localization)을 공동 설계하였다. 이 알고리즘의 핵심 전제는 "UWB와 WiFi 두 이종 센서가 동일한 위치를 동시에 지지할 때 신뢰도가 높다"는 이종 센서 합의도 가정이었다. Part 2에서 두 센서의 거리 차이를 가우시안 합의도로 수치화하고, Part 3에서 이를 결합 가중치로 변환한 뒤, Part 4에서 Grid Search + Huber Loss + L-BFGS-B로 초기 위치를 추정하고, Part 5에서 IRWLS + Tukey Biweight로 정제하는 5단계 파이프라인이었다. Test Set(N=270) 기준 MAE 1.79 m를 기록하였다.

그러나 Final Project 데이터는 18개 기지국이 모두 동일한 RTT 거리만 제공하는 단일(homogeneous) 측정 구조이다. UWB와 WiFi 구분이 존재하지 않으므로 SAFL의 이종 센서 합의도 가정 자체가 성립하지 않는다. 두 환경의 핵심 차이는 다음과 같다.

| 항목 | 중간발표 환경 | Final Project 환경 |
|---|---|---|
| 센서 구성 | UWB + WiFi (이종) | RTT 단일 (동종) |
| 앵커 수 | 12개 (UWB 6 + WiFi 6) | 18개 |
| 공간 규모 | 약 12 m × 17 m | 약 120 m × 60 m |
| NLOS bias 규모 | 수 m 수준 | 평균 +15.93 m |
| 핵심 아이디어 적용 여부 | 이종 센서 합의 | 적용 불가 |

따라서 중간발표 알고리즘을 단순 발전시키는 방향이 아니라, Final Project 데이터 특성을 처음부터 분석하여 새로운 알고리즘을 설계하는 방향으로 전환하였다.

## 1.2 중간발표 피드백 수용

중간발표에서 받은 피드백은 다음과 같이 반영하였다.

| 피드백 | 반영 내용 |
|---|---|
| 파이프라인이 과도하게 복잡 | Part 2(이종 센서 합의도)와 Part 3(boost 기반 가중치)를 제거하고 물리 성질만 활용하는 단순 구조로 재설계 |
| 불필요한 구성 요소 포함 | 각 구성 요소를 ablation 실험으로 검증하여 기여가 없는 요소 제거 |
| 각 요소가 성능에 기여하는지 검증 필요 | §4에서 단계별 ablation 수치로 각 요소의 기여도를 정량 제시 |
| 결과의 의미 해석 요구 | 수치 나열이 아닌 각 수치가 무엇을 의미하는지 §4에서 구체적으로 해석 |

## 1.3 데이터 특성 분석과 설계 원리 도출

학습 데이터 700명에 대해 18개 앵커 각각의 RTT 측정값과 GT 위치 기반 실제 거리의 차이(NLOS bias)를 분석하였다. 결과는 다음과 같다.

| 특성 | 값 |
|---|---|
| 공간 범위 | x = [-60, 60] m, y = [-30, 30] m |
| 기지국 배치 범위 | x = [-50, 50] m, y = [-20, 20] m |
| d_hat 범위 | 3.36 ~ 307.34 m |
| d_hat 평균 / 중앙값 | 63.83 m / 58.19 m |
| NLOS bias 평균 / 중앙값 | +15.93 m / +10.15 m |
| bias > 0 비율 | 81.4 % |
| bias > 5 m 비율 | 59.0 % |
| bias > 10 m 비율 | 50.3 % |
| 앵커별 median bias 범위 | 6.37 m ~ 13.14 m |

이 분석에서 알고리즘 설계 전체를 지배하는 물리적 사실이 도출된다. NLOS 환경에서는 신호가 장애물에 의해 반사·회절 경로를 통해 도달하므로 RTT 측정값이 실제 거리보다 항상 크거나 같다. 즉 d_hat[i] >= d_true[i]가 성립한다. bias > 0 비율이 100%가 아니라 81.4%인 이유는 NLOS 양의 편향 위에 타임스탬프 분해능·클록 동기화 잡음이 더해지기 때문이다. LOS에 가까운 측정에서는 이 영평균 잡음이 가끔 음의 값을 만들어 d_hat < d_true가 된다. 그러나 이는 잡음에 의한 예외이고, 지배적인 방향은 양의 편향이다.

이 물리적 사실로부터 두 가지 설계 원리를 도출하였다.

원리 A (NLOS는 거리를 늘린다): 편향이 양수이므로, 상대적으로 작은 측정값일수록 LOS에 가까울 가능성이 높다.

원리 B (측정값은 실제 거리의 상한이다): d_hat[i] >= d_true[i]는 곧 ||x_true - b_i|| <= d_hat[i]를 의미한다. 실제 위치는 각 기지국에서 반지름 d_hat[i]인 원(disk)의 내부에 있어야 한다. 따라서 18개 측정 원의 교집합이 위치 후보 영역이다.

원리 B에서 핵심 아이디어가 도출된다. 18개 원의 교집합에서 "가장 깊은 내부점"인 Chebyshev Center를 찾으면, bias를 명시적으로 추정하지 않고도 실제 위치에 가장 안전하게 수렴할 수 있다.

## 1.4 Bias 보정 접근의 한계 확인

Minimax로 방향을 전환하기 전에, 기존 SAFL 구조에서 Part 2·3을 제거하고 앵커별 median bias 보정을 적용한 결과를 먼저 확인하였다. bias 보정 후에도 잔존 오차의 중앙값이 9.94 m에 달하였는데, 이는 앵커별 bias가 위치에 따라 달라지는 위치 의존적 특성을 고정 통계량으로 보정할 수 없기 때문이다.

18개 앵커의 median bias가 6.37 m에서 13.14 m로 앵커마다 최대 2배 이상 차이가 나고, 같은 앵커라도 사용자 위치마다 NLOS 경로의 성격이 달라 bias 분산이 매우 크다. 고정 bias 보정 후 SAFL(Grid + Huber + IRWLS)을 적용하여도 MAE 9.55 m에 그쳤다. 이 결과가 bias 추정을 포기하고 물리 성질 직접 활용 방향으로 전환한 직접적 동기이다.

## 1.5 알고리즘 아이디어 도출 흐름

여러 방법론을 탐색하는 과정에서 다음 실험들을 순서대로 수행하였다.

| 실험 순서 | 시도한 방법 | MAE | 결론 |
|---|---|---:|---|
| 1 | Simple LS (raw d_hat) | 39.35 m | NLOS 처리 필수 |
| 2 | Bias보정 + LS | 32.48 m | 고정 bias 한계 |
| 3 | SAFL (bias보정 + Grid + Huber + IRWLS) | 9.55 m | 잔존 오차 여전히 큼 |
| 4 | 삼각부등식 outlier 제거 시도 | 악화 | LOS 앵커 38.4% 잘못 제거 |
| 5 | Minimax (Chebyshev Center) 단독 | 6.73 m | bias 없이 NLOS 성질 직접 활용 |
| 6 | Minimax + LTS 정밀화 | 더 개선 | 잔차 하위 7개 앵커만 fit |
| 7 | Minimax + LTS + Slack Fallback | 4.80 m | 최종 구조 확정 |

삼각부등식 시도에서는 bias 보정 후에도 d_corr의 표준편차가 32.9 m로 앵커 간 최대 거리 107.7 m보다 크지 않아 LOS 앵커를 38.4% 잘못 제거하는 문제가 발생하였다. 이 실패 경험이 측정값 간 일관성 대신 물리 성질(NLOS 양의 편향)을 직접 목적함수에 반영하는 방향으로 전환하는 계기가 되었다.

## 1.6 알고리즘 high-level 소개

최종 알고리즘은 세 단계로 구성된다. Step 1에서는 NLOS 양의 편향 성질을 이용하여 18개 측정 원의 가장 깊은 내부점(Chebyshev Center)을 Minimax 최적화로 탐색한다. Step 2에서는 그 결과를 초기점으로 LTS로 잔차 하위 7개 앵커에만 fit하여 위치를 정밀화한다. Step 3에서는 LTS 결과가 측정 원 밖으로 이탈하는 경우 Minimax 결과로 복귀하는 Slack 기반 Fallback을 적용한다. GT 위치를 일체 사용하지 않는 완전 비지도 방법으로, hidden test set에 대한 일반화 성능이 구조적으로 안정적이다.

---

# 2. 알고리즘 설명

입력은 UE 한 명에 대한 RTT 기반 측정 거리 벡터 d = (d_1, ..., d_18)와 18개 기지국 좌표 B = {b_i = (x_i, y_i) | i = 1, ..., 18}이다. 출력은 추정 위치 x_hat = (x, y) ∈ R^2이다. 기지국까지의 기하 거리를 rho_i(q) = ||q - b_i||_2로, 잔차를 r_i(q) = rho_i(q) - d_i로 정의한다.

## Step 0. 탐색 공간 설정 (오프라인, 1회)

기지국 좌표의 x, y 방향 범위를 W_x = x_max - x_min, W_y = y_max - y_min으로 계산하고 각 방향으로 30%를 마진으로 추가하여 탐색 경계를 설정한다.

B_x = [x_min - 0.30 * W_x, x_max + 0.30 * W_x]

B_y = [y_min - 0.30 * W_y, y_max + 0.30 * W_y]

이 경계 내부에 0.5 m 간격의 2차원 격자 G를 구성한다. |G|는 약 41,409개 점이며, main() 진입 시 1회 계산하여 모든 UE 추론에 재사용한다. 격자는 기지국 좌표만으로 결정되며 GT 위치나 학습 데이터가 필요 없다.

## Step 1. Minimax (Chebyshev Center)

### 이론적 근거

d_hat[i] >= d_true[i]가 성립하므로 실제 위치는 18개 측정 원 D_i = {q : ||q - b_i|| <= d_i}의 교집합 내부에 반드시 존재한다. 18개 원의 교집합에서 "가장 깊은 내부점"인 Chebyshev Center는 이 교집합에서 경계까지의 거리가 가장 큰 점으로, 어떤 앵커의 측정 오차에도 가장 강건한 위치이다.

### 목적함수 정의

후보 위치 q에서 기지국 i에 대한 slack을 다음과 같이 정의한다.

s_i(q) = ||q - b_i|| - d_i

s_i(q) < 0이면 q가 원 D_i의 내부, s_i(q) > 0이면 외부(위반)를 의미한다. Chebyshev Center는 최악의 위반을 최소화하는 점이다.

f(q) = min_q max_i s_i(q) = min_q max_i (||q - b_i|| - d_i)

### 수식 변형 검토

max 연산은 미분 불가능하므로 smooth 근사가 필요하다. 팀원 피드백에서 절댓값 수식 min_q max_i |s_i(q)|, 단방향 수식 min_q max_i max(0, d_i - ||q-b_i||) 등 네 가지 변형을 제안받아 실험하였다. 결과는 다음과 같다.

| 수식 변형 | MAE (Minimax + LTS + fallback) | 채택 여부 |
|---|---:|---|
| v1: max(||q-b_i|| - d_i) | 4.800 m | 채택 |
| v2: max |  ||q-b_i|| - d_i  | | 26.293 m | 기각 |
| v3: max(0, ||q-b_i|| - d_i) | 5.920 m | 기각 |
| v4: max(0, d_i - ||q-b_i||) | 44.330 m | 기각 |

v4가 더 자연스럽다는 의견이 있었으나, v4는 측정값에 맞추기 위해 원 경계 방향으로 q를 끌어당겨 NLOS 측정값에 직접 fit하는 문제가 발생하였다. v1이 모든 수식 중 최선임을 실험으로 확인하여 v1을 채택하였다.

### Log-sum-exp 근사 및 최적화

max 연산의 smooth 근사로 다음 log-sum-exp를 사용한다. alpha = 10으로 설정하면 max에 충분히 가까운 근사를 제공하면서 L-BFGS-B 적용이 가능해진다.

f_smooth(q) = (1 / alpha) * log( sum_{i=1}^{18} exp(alpha * s_i(q)) )

격자 G의 모든 점에서 f_smooth를 평가하여 값이 가장 작은 격자점을 초기점 q_0으로 선택한다. 격자 탐색은 비볼록 목적함수에서 전역 최솟값 근방을 포착하기 위한 것이다. 이후 L-BFGS-B로 탐색 경계 B_x x B_y 내에서 수치 최소화하여 p_minimax를 확정한다.

## Step 2. Least Trimmed Squares (LTS) 정밀화

### 이론적 근거

p_minimax는 모든 원 내부의 안전한 점이지만, 원 교집합의 깊은 내부에 위치하므로 실제 사용자 위치와 수 m 거리가 있을 수 있다. LTS는 잔차 제곱값이 가장 작은 k개 앵커에만 fit하여 NLOS 심한 앵커를 자동으로 배제하고 위치를 정밀화한다. 이 방식은 Huber Loss처럼 연속 가중치를 사용하지 않고, 하위 k개 앵커만 선택하여 나머지를 완전히 무시하는 강한 outlier 배제이다.

### 목적함수

잔차 r_i(q) = ||q - b_i|| - d_i의 제곱값을 오름차순으로 정렬한 r_(1)^2 <= r_(2)^2 <= ... <= r_(18)^2에 대해 LTS 목적함수는 다음과 같다.

L_LTS(q) = sum_{j=1}^{k} r_(j)^2(q)

k = 7로 설정하면 18개 중 잔차가 작은 7개 앵커에만 fit하고, NLOS가 심한 나머지 11개 앵커를 자동으로 무시하는 효과를 가진다. p_minimax를 초기점으로 L-BFGS-B로 최소화하여 p_lts를 확정한다.

### k 선정 근거

k = 7은 18개 중 약 39%에만 fit한다. 이는 bias > 5 m 비율이 59.0%라는 데이터 분석 결과에 근거한다. 전체 측정의 과반이 심한 NLOS 상태이므로, 잔차 하위 약 40%만 신뢰하는 것이 합리적이다. k를 5, 7, 9로 바꾸며 실험한 결과, k = 7에서 MAE와 2m이내 비율의 균형이 가장 좋았다.

## Step 3. Slack 기반 Fallback

### 필요성

LTS 정밀화는 p_minimax를 초기점으로 잔차 작은 7개 앵커에 맞추는 과정에서, 강한 NLOS 앵커 방향으로 끌려가 측정 원 밖으로 이탈할 수 있다. 이는 NLOS 물리 성질(d_hat >= d_true, 즉 실제 위치는 모든 원 내부)에 위배된다. 700명 중 207명(29.6%)에서 이 이탈이 발생하였다.

### 판정 기준

p_lts에서 모든 기지국에 대한 slack의 최댓값을 계산한다.

max_slack = max_{i=1,...,18} (||p_lts - b_i|| - d_i)

max_slack > 5 m이면 p_lts가 NLOS 물리 성질을 위반한 것으로 판단하여 p_minimax를 최종 추정값으로 반환한다. max_slack <= 5 m이면 p_lts를 최종 추정값으로 반환한다. threshold 5 m는 LOS 측정에 섞이는 잡음 수준(|bias| < 1 m 비율 29.9%)을 고려하여, 작은 위반은 허용하되 명확한 이탈만 차단하도록 설정하였다.

## Step별 처리 요약

| 단계 | 입력 | 연산 | 출력 |
|---|---|---|---|
| Step 0 | BS_positions | 0.5m 격자 구성 + 탐색 경계 설정 | 격자 G, 경계 B |
| Step 1 | d_vec, G, B | 격자 탐색 → log-sum-exp L-BFGS-B | p_minimax |
| Step 2 | p_minimax, d_vec, B | LTS 목적함수 L-BFGS-B (k=7) | p_lts |
| Step 3 | p_minimax, p_lts, d_vec | max slack 계산 → 5m 비교 | p_hat |

## 비지도 특성

본 알고리즘은 어떤 단계에서도 GT 위치(p)를 사용하지 않는다. 탐색 공간은 BS_positions만으로 결정되고, Minimax와 LTS는 측정값 d_hat과 BS_positions만을 입력으로 사용한다. Fallback 기준 5 m 역시 측정값과 기하 거리의 일관성만으로 판단한다. 따라서 hidden test set에 대해 train set 정보 유출이 구조적으로 불가능하다.

---

# 3. Agent AI 활용 방안

본 프로젝트에서는 Claude(Anthropic)를 주요 Agent AI로 활용하였다.

## 3.1 본인과 Claude의 역할 구분

| 작업 | 본인의 역할 | Claude의 역할 |
|---|---|---|
| 문제 재정의 | NLOS 양의 편향 성질 발견 및 Minimax 아이디어 도출 | 여러 방법론(삼각부등식, LMedS, LTS, Rank-based 등) 개념 설명 |
| 실험 설계 | 어떤 방법을 시도할지 결정, 비교 baseline 설계 | 코드 작성 및 실행, 성능 수치 측정 |
| 결과 해석 | 수치를 보고 방향 전환 여부 판단 | 수치 정리 및 비교표 작성 |
| 수식 결정 | v1~v4 중 v1 채택 판단 | 네 수식 구현 및 성능 측정 |
| 파라미터 결정 | alpha=10, k=7, slack threshold=5 m 결정 | 각 파라미터 범위 실험 및 결과 제시 |
| 코드 작성 | 알고리즘 설계 방향 지시, README 규격 검토 | main.py 최종 구현 |
| 보고서 | 실험 흐름 및 설계 의도 제공 | 문장 구성 보조, 수치 표 작성 |

## 3.2 구체적 활용 사례

### 삼각부등식 실험과 방향 전환

삼각부등식 기반 outlier 제거를 시도할 때 Claude가 실험을 구현하고 실행하였다. 결과로 "d_corr 표준편차 32.9 m, 앵커 간 최대 거리 107.7 m, 실제 LOS 앵커를 38.4% 잘못 제거, 기존 대비 MAE 악화"를 제시하였다. 이 수치를 보고 본인이 "측정값 간 기하 일관성 기반 접근은 이 데이터의 극심한 NLOS에서 근본적으로 한계가 있다"고 판단하고, 물리 성질을 직접 활용하는 Minimax 방향으로 전환을 결정하였다.

### 수식 변형 실험

팀원으로부터 v4(max(0, d_i - ||q-b_i||)) 수식이 더 자연스럽다는 피드백을 받았다. Claude가 v1~v4 네 가지 수식을 각각 구현하고 "LTS + fallback" 구조에서의 성능을 측정하였다. 결과에서 v4가 MAE 44.33 m로 가장 나쁨을 확인하고 본인이 v1 유지를 결정하였다. v4가 나쁜 이유도 Claude와 분석하였는데, v4는 실제 위치가 원 내부에 있다는 성질 대신 측정값에 맞추는 방향으로 최적화하여 NLOS 측정값에 직접 끌려가는 것이 원인이었다.

### k값과 threshold 결정

LTS의 k를 5, 7, 9로, Slack threshold를 0, 5, 10 m로 바꾸며 Claude가 성능 측정을 수행하였다. 본인이 "bias > 5 m 비율 59.0%이므로 전체의 약 40%인 7개만 신뢰하는 것이 적합하다"는 물리적 근거로 k = 7을, "LOS 잡음 수준을 고려해 작은 위반은 허용해야 한다"는 이유로 threshold = 5 m를 선택하였다.

### AI 제안의 기각 사례

Claude가 k-NN 적응 bias 보정 방법을 제안하였다. 학습 데이터 d_hat 패턴이 유사한 이웃을 찾아 그들의 bias를 가중 평균하여 보정하는 방식이었다. 실험 결과 MAE 7.76 m로 개선이 있었으나, 이 방법은 GT 위치(p)를 학습 데이터에서 직접 참조하므로 완전 비지도가 아니라는 판단으로 본인이 채택하지 않았다.

---

# 4. 결과 도출 & 디스커션

## 4.1 자체 평가 방식

제공된 학습 데이터 700명 전체를 평가에 사용하였다. 본 알고리즘은 GT 위치를 일체 사용하지 않으므로 학습/테스트 분리가 필요 없다. 700명 전체에서 평가한 결과가 hidden test set에서의 성능을 공정하게 근사한다.

평가 지표는 MAE(평균 절대 오차), Median Error(50번째 백분위수), RMSE(제곱평균제곱근 오차), P90(90번째 백분위수 오차), 2 m 이내 비율을 사용하였다. Median은 outlier에 강건한 지표로 전형적인 케이스 성능을 반영하고, P90은 tail 케이스 성능을 반영한다.

## 4.2 Baseline 비교 설계와 공정성

비교 대상을 모두 비지도 또는 통계량만 사용하는 방법으로 구성하였다. 단계별 ablation을 통해 각 구성 요소의 기여도를 분리하였다.

| 방법 | 설명 | GT 사용 여부 |
|---|---|---|
| Simple LS | raw d_hat으로 최소제곱 삼변측량 | 없음 |
| Bias보정 + LS | 앵커별 median bias 차감 후 LS | 통계량만 (GT 간접 사용) |
| SAFL (중간발표) | Bias보정 + Grid + Huber + IRWLS | 통계량만 |
| Minimax only | Step 1만 수행 (ablation) | 없음 |
| 제안 알고리즘 (Full) | Step 1 + Step 2 + Step 3 | 없음 |

비교의 공정성 측면에서, 본 알고리즘은 GT를 전혀 사용하지 않으므로 Bias보정 계열보다 불리한 조건이다. 그럼에도 SAFL보다 MAE, Median 모두 우수하였다. GT를 전혀 사용하지 않는 Simple LS와의 비교가 가장 fair하며, MAE 39.35 m에서 4.80 m로의 개선이 알고리즘의 실질적 기여를 보여준다.

## 4.3 실험 결과

| 방법 | MAE (m) | Median (m) | RMSE (m) | P90 (m) | 2m 이내 (%) |
|---|---:|---:|---:|---:|---:|
| Simple LS (raw) | 39.352 | 27.054 | 61.542 | 72.629 | 0.3 |
| Bias보정 + LS | 32.479 | 21.106 | 53.670 | 58.971 | 0.4 |
| SAFL (중간발표) | 9.553 | 8.195 | 11.790 | 17.664 | 3.6 |
| Minimax only (Step 1) | 6.727 | 5.270 | 9.210 | 15.324 | 35.3 |
| 제안 알고리즘 (Full) | 4.800 | 1.261 | 7.990 | 14.714 | 58.7 |

## 4.4 단계별 결과 해석

Simple LS는 NLOS bias를 전혀 처리하지 않아 MAE 39.35 m로 가장 나쁘다. 이 수치는 bias 처리의 필요성을 보여주는 기준값이다.

Bias보정 + LS는 앵커별 고정 median bias를 차감하지만, 위치에 따라 달라지는 NLOS 경로 특성을 단일 통계량으로 보정할 수 없어 MAE 32.48 m에 그친다. 개선 폭이 작은 것은 bias 변동성이 앵커 간 최대 2배(6.37~13.14 m)에 달하고, 같은 앵커라도 UE 위치마다 다르기 때문이다.

SAFL은 Huber Loss와 IRWLS로 outlier 영향을 억제하여 MAE 9.55 m를 달성한다. 하지만 bias 보정 후에도 잔존 오차 중앙값이 9.94 m에 달해, 이후 최적화 단계에서 수렴이 불안정하다.

Minimax only는 bias 추정 없이 NLOS 물리 성질만으로 MAE 6.73 m, 2 m 이내 35.3%를 달성한다. bias 보정 기반인 SAFL보다 오히려 2.8 m 우수하다. 이는 Chebyshev Center가 LOS에 가까운 앵커들의 원 교집합 깊은 내부로 자연스럽게 수렴하기 때문이다. 그러나 Median이 5.27 m로, 다수 샘플에서 전형적 오차가 크다.

LTS 정밀화(Step 2) 추가 후 Median이 5.27 m에서 1.26 m로 크게 개선되고 2 m 이내 비율이 58.7%로 상승한다. 이는 p_minimax를 초기점으로 잔차 하위 7개 앵커에만 fit하여 NLOS 심한 11개 앵커를 자동으로 배제하기 때문이다. 700명 중 493명(70.4%)에서 LTS가 정상 작동하였다.

Slack Fallback(Step 3)은 나머지 207명(29.6%)에서 LTS가 원 밖으로 이탈하는 경우 p_minimax로 복귀하여 MAE 안정성을 확보한다. Fallback 없이 LTS만 적용하면 일부 샘플에서 수백 m 폭발이 발생하여 MAE가 크게 상승하였으며, Fallback이 이를 방지한다.

## 4.5 알고리즘 장점

완전 비지도 구조로 학습 데이터 과적합 위험이 없다. NLOS 양의 편향이라는 물리적 사실만을 가정하므로, 환경이 달라도 이 성질이 유지되는 한 성능이 안정적이다. 파라미터가 alpha = 10, k = 7, threshold = 5 m 세 개뿐이며 모두 물리적 근거로 선정되어 환경 의존성이 낮다. 1000명 처리 시간이 약 15초로 10분 제한 대비 40배 여유가 있다.

## 4.6 알고리즘 단점

LTS의 k = 7은 18개 중 11개 이하가 NLOS outlier라는 가정에 기반한다. 극단적 환경에서 outlier 앵커가 12개 이상이면 LTS가 실패할 수 있으며, Fallback이 보완하지만 완전한 해결은 아니다. P90이 14.71 m로 일부 샘플에서 큰 오차가 잔존한다. Minimax의 log-sum-exp 근사(alpha = 10)가 수치적으로 불안정한 경우(exp overflow)가 드물게 발생할 수 있다.

## 4.7 본인의 사고와 구현의 적합성

Minimax가 bias 추정 없이 NLOS 물리 성질만으로 SAFL보다 우수한 결과를 낸 것은, 이 데이터에서 bias의 위치 의존성이 너무 커서 통계량 기반 보정이 오히려 해롭다는 판단이 적중한 결과이다. LTS가 IRWLS보다 더 강한 outlier 배제를 제공하여 Median을 크게 개선한 것도, raw d_hat에서 잔차가 Huber 전환점(5 m)을 훨씬 넘는 경우가 많다는 데이터 분석 결과와 일치한다.

## 4.8 Future Work

k를 고정하는 대신 Minimax 결과에서의 잔차 분포를 분석하여 샘플별로 동적으로 결정하는 방법을 고려할 수 있다. 예를 들어 잔차의 MAD를 기준으로 inlier 임계값을 설정하고 해당 임계값 이하의 앵커 수를 k로 사용하면 데이터 적응적 LTS가 가능하다. Slack Fallback threshold도 고정값 대신 해당 샘플의 d_hat 중앙값에 비례하여 설정하면 환경에 더 강건한 판단이 가능하다.

---

# 5. Reference

[1] P. C. Chen, "A non-line-of-sight error mitigation algorithm in location estimation," Proc. IEEE Wireless Communications and Networking Conference (WCNC), 1999.

이 논문은 NLOS 환경에서 RTT 측정값이 실제 거리보다 크게 측정되는 현상을 분석하고, 측정 잔차의 통계적 분포(분산, 왜도 등)를 이용하여 NLOS 여부를 hypothesis test로 식별한 뒤 보정 계수를 적용하는 방법을 제안한다.

본 알고리즘이 이 논문에서 참고한 핵심 부분은 NLOS 측정값이 실제 거리보다 크다는 물리적 관찰이다. 이 관찰에서 d_hat[i] >= d_true[i], 즉 ||x_true - b_i|| <= d_hat[i]라는 부등식 성질이 도출되며, 이것이 Minimax 목적함수 설계의 출발점이 되었다. §2의 Step 1에서 이 성질을 slack s_i(q) = ||q - b_i|| - d_i로 표현하고 최솟값을 찾는 것이 직접적 연결이다.

본 알고리즘이 이 논문과 다른 부분은 NLOS 식별 방법이다. 이 논문은 잔차 분포를 분석하여 NLOS를 이진 분류하고 보정 계수를 추정한다. 반면 본 알고리즘은 NLOS를 분류하거나 보정하지 않고, d_hat >= d_true라는 성질을 목적함수에 직접 반영하여 Chebyshev Center를 탐색한다. 이 차이가 GT 위치 없이도 동작하는 완전 비지도 구조를 가능하게 한다.

[2] S. Maranò, W. M. Gifford, H. Wymeersch, and M. Z. Win, "NLOS Identification and Mitigation for Localization Based on UWB Experimental Data," IEEE Journal on Selected Areas in Communications, vol. 28, no. 7, pp. 1026-1035, 2010.

이 논문은 UWB 실험 데이터를 기반으로 NLOS 측정값이 양의 편향을 가지며 분산이 크다는 통계적 특성을 실증적으로 보이고, 이를 식별하여 위치 추정 정확도를 높이는 방법을 제안한다. 특히 NLOS 측정값을 단방향 부등식 제약(soft upper bound constraint)으로 처리하는 아이디어를 제시한다.

본 알고리즘이 이 논문에서 참고한 부분은 두 가지이다. 첫째, NLOS 측정값이 실제 거리의 상한(upper bound)으로 작동한다는 관점이다. d_hat[i]를 ||x - b_i||의 상한으로 해석하면, 실제 위치는 각 측정 원 내부에 있어야 한다는 기하학적 결론으로 이어진다. 이것이 §2 Step 1에서 18개 측정 원의 교집합 내부에서 위치를 탐색하는 근거이다. 둘째, 이 논문의 데이터 분석에서 bias > 0 비율이 대다수라는 실증적 뒷받침을 확인하였으며, 본 데이터 분석(bias > 0 비율 81.4%)과 일치함을 확인하였다.

본 알고리즘이 이 논문과 다른 부분은 구현 방식이다. 이 논문은 NLOS를 통계적으로 먼저 식별한 뒤 식별된 측정값에 부등식 제약을 부과하는 2단계 방식이다. 반면 본 알고리즘은 NLOS 식별 과정 없이 모든 18개 측정값을 상한 제약으로 동시에 간주하여 Minimax 최적화를 수행한다. 이후 LTS로 잔차 하위 7개 앵커만 fit하는 방식은 이 논문의 식별 단계를 대체한다. 이 논문이 식별 실패 케이스에서 성능이 저하되는 반면, 본 알고리즘은 식별 없이 Slack Fallback으로 실패 케이스를 처리한다.

[3] P. J. Huber, "Robust Estimation of a Location Parameter," Annals of Mathematical Statistics, vol. 35, no. 1, pp. 73-101, 1964.

이 논문은 M-estimator 개념을 도입하여 outlier에 강건한 위치 추정 방법론의 이론적 토대를 제시한다. Huber Loss는 잔차 절댓값이 임계값 이하이면 제곱 손실, 초과이면 선형 손실을 적용하여 outlier의 제곱 벌점화를 방지한다.

본 알고리즘이 이 논문에서 참고한 부분은 outlier를 완전히 제거하지 않고 잔차 크기에 따라 차등 취급하는 robust estimation의 철학이다. 중간발표 SAFL에서 Part 4에 Huber Loss를 적용하여 NLOS 앵커의 영향을 제한하였으며, 이 논문의 방법론을 직접 구현한 것이다.

본 알고리즘에서 Huber Loss 대신 LTS를 채택한 이유는 실험적으로 확인한 두 가지 사실에 근거한다. 첫째, raw d_hat을 그대로 사용하는 본 알고리즘에서 잔차가 Huber 전환점(5 m)을 훨씬 넘는 경우가 많아 Huber의 선형 페널티 구간이 outlier 억제 효과를 충분히 발휘하지 못한다. 둘째, LTS는 잔차 하위 k개만 fit하여 나머지를 완전히 무시하는 더 강한 outlier 배제가 가능하다. Huber는 outlier에도 선형 페널티를 부여하여 여전히 영향을 받지만, LTS는 threshold 이상의 잔차를 가진 앵커를 목적함수에서 완전히 제외한다. 이 차이가 Median 지표에서 두드러지게 나타났으며, LTS가 더 적합하다는 실험적 확인에 근거하여 채택하였다.
